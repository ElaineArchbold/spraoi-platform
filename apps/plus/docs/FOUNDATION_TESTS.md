# Foundation v1 tests

1. App loads locally.
2. Missing env vars show a friendly warning.
3. Parent/Guardian can sign in.
4. Squad selector appears.
5. Parent sees only linked children for selected squad.
6. Admin with `user_roles.role = admin` sees admin foundation.
7. SuperAdmin with `user_roles.role = super_admin` can see admin foundation.
8. Existing live apps are untouched.

Next migration:
- Exact weekly plans/videos per squad.
- Friday Night Hurling only for 2014 Boys and 2015 Girls.
- GPS logger and saved run modal.
