# Spraoi Connect

This module did not have a standalone source application in the uploaded workspace. Build it here using the shared module shell after Coach and Journey are stabilised. The Figma design-system preview remains in `apps/design-system-preview`.
